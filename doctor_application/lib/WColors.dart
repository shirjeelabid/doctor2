import 'package:flutter/material.dart';

class WColors {
  Color appBar;
  Color textColor;
  Color appBarText;
  Color appBarIcons;
  Color bottomBarIcons;
  Color flatButton;
  Color buttonTextColor;
  Color checkBox;
  Color raisedButton;

  WColors() {
    appBarIcons = Color(0x00000000);
    appBarText = Color(0xFFFFFFFF);
    appBar = Color(0xFF00AAF7);
    textColor = Color(0xff000000);
    bottomBarIcons = Colors.grey;
    flatButton = Color(0xff3126c9);
    buttonTextColor = Color(0xFFFFFFFF);
    checkBox = Colors.orange;
    raisedButton = Color(0xff44eb4a);
  }
}
